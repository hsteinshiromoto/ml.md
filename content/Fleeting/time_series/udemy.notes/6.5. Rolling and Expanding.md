* `df[feature].rolling(window=n_rows).mean()`: Rolling mean using `n_rows`
* `df[feature].expanding().mean()`: Calculates the average value with expanding window. The window keeps expanding with the data.
